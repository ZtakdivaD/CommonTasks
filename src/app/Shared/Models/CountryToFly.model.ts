export class CountryToFlyModel {
  constructor(
    id: number,
    destination: string,
    departure: string,
    arriveAt: string,
    price: number,
    country: string,
    isSelected: boolean
  ) {

  }
}
